#include <iostream>

using namespace std;

/*
    Programma che viede e somma due numeri interi POSITIVI ma che avverta
    che il risultato non è attendibile in caso di overflow
*/

int main()
{
    const int MAXINT = sizeof(int);

    int a, b;

    int c = a + b;

    cout << c;
    if ()
    {
        /* code */
    }
    
    cout << "Il risultato non è attendibile" << endl;

}